// ===== ไม่ใช้แล้วในระบบใหม่ =====
// ระบบใหม่ไม่มีการให้ owner เพิ่มหอพักเอง
// คนทั่วไปจะส่งข้อมูลผ่านฟอร์ม แล้วแอดมินอนุมัติ
// ไฟล์นี้จะถูกแทนที่ด้วย submissionController.js

/*
// Original AddDormitoryController.js - ไม่ใช้แล้ว
const pool = require("../db");
const storageService = require("../services/r2StorageService");

// ... rest of the original code ...
// (คอมเม้นต์ทั้งไฟล์เพราะไม่ใช้แล้ว)
*/

module.exports = {
  // ไม่มี exports เพราะไม่ใช้แล้ว
};